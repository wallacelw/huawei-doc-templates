% =====================================================================
%  Test Book — Amostra em português (verifica todos os recursos do template)
%  Compilar com XeLaTeX:
%      xelatex main.tex   (executar 2x para o sumário)
%
%  Opções de classe:
%    portuguese  — rótulos em português (padrão: inglês)
%    notime      — oculta o horário de compilação na capa
%    nochangelog — desabilita a renderização do histórico de versões
%    noauthors   — oculta os autores na capa
%    noanswers   — oculta Observações e Resultado do Teste nos casos de teste
% =====================================================================
\documentclass[portuguese]{testbook}

% --- Configuração do documento --------------------------------------
\settestbooktitle{Casos de Teste POC — Validação da Plataforma de Dados}
\setheadertitle{Huawei Cloud -- Casos de Teste POC}
\setcovertext{Huawei Technologies CO., LTD}
\setdocversion{1.1.0}
\setdocdate{\today}

\begin{document}

% ---- CAPA -----------------------------------------------------------
\makecover

% ---- SUMÁRIO --------------------------------------------------------
\maketoc

% ---- CORPO (reinicia a numeração de páginas em 1) -------------------
\startbody

\begin{infobox}
Este documento demonstra o template \inlinecode{testbook} (\inlinecode{testbook.cls}) para
documentos de casos de teste POC/aceitação do Huawei Cloud. Ele exercita todos os
comandos e ambientes disponíveis: capa, sumário, ambiente de caso de teste
com todos os comandos de campo, bloco de objetivos, blocos de código, imagens,
caixas de destaque (\inlinecode{warning}, \inlinecode{tip}, \inlinecode{infobox}), tabelas, badge,
caminhos de menu, weblinks, notas, referências param, tabela de resumo dos testes,
tabelas de passos dos testes, emblemas de resultado e histórico de versões.
\end{infobox}

% =====================================================================
%  Seção 1 — Descrição dos Casos de Teste
% =====================================================================
\section{Descrição dos Casos de Teste}

\begin{objectives}
  \generalobjective{Validar a implantação da Plataforma de Dados do Huawei Cloud para o
    ambiente de POC, garantindo que todos os serviços principais (DataArts Studio, MRS,
    OBS, VPC) estejam provisionados corretamente, acessíveis e funcionando conforme
    especificado na arquitetura da solução.}

  \prerequisites
  \begin{itemize}
    \item Conta Huawei Cloud com privilégios de administrador IAM na região \inlinecode{sa-brazil-1}.
    \item VPC, sub-rede e grupos de segurança já criados pela equipe de infraestrutura.
    \item Instância do DataArts Studio (Dayu) provisionada e licenciada.
    \item Cluster MRS implantado com a pilha de componentes necessária (HDFS, YARN, Spark, Hive).
    \item Bucket OBS criado e acessível com a política IAM designada.
    \item Ambiente de execução de testes com conectividade de rede ao Console do Huawei Cloud.
    \item Ferramentas CLI \inlinecode{latexmk} e \inlinecode{terraform} instaladas localmente.
  \end{itemize}
\end{objectives}

\subsection{Escopo dos Testes}

\begin{table}[H]
  \begin{hutable}{|l|p{0.72\textwidth}|}
    \rowcolor{huaweired} \thd{Domínio} & \thd{O Que Deve Ser Observado} \\
    \tbody
    Arquitetura da Plataforma & Os serviços principais estão implantados, saudáveis e acessíveis via API do Console. \\
    Engenharia de Dados & Os pipelines do DataArts Studio conseguem ingerir, transformar e carregar dados no armazenamento de destino. \\
    Armazenamento de Dados & Os buckets OBS e o HDFS do MRS aceitam escritas/leituras com as permissões corretas. \\
    Segurança & As políticas IAM, o isolamento VPC e as regras dos grupos de segurança impõem o controle de acesso pretendido. \\
  \end{hutable}
  \caption{Escopo dos testes por domínio.}
\end{table}

\subsection{Pré-condições e Preparações}

\begin{itemize}
  \item Todos os recursos de infraestrutura devem ser provisionados via Terraform e a saída de \inlinecode{terraform apply} deve apresentar zero erros.
  \item O cluster MRS deve estar no estado \textbf{Executando} antes da execução de qualquer caso de teste de engenharia de dados.
  \item Os arquivos de dados de teste (CSV/JSON) devem ser carregados no bucket OBS designado antes da execução dos pipelines.
  \item Cada testador deve ter uma conta IAM pessoal com a função \textbf{Dayu\_User} atribuída no DataArts Studio.
  \item As regras de firewall de rede devem permitir TCP 22 (SSH) e TCP 443 (HTTPS) de entrada a partir da sub-rede de execução de testes.
\end{itemize}

\note{Certifique-se de que o ambiente de execução de testes esteja configurado com o fuso horário \inlinecode{America/Sao\_Paulo} (GMT-3) para que os registros de data/hora nos logs estejam alinhados com o Console do Huawei Cloud.}

\begin{code}[bash]
# Verificar se as ferramentas CLI estão instaladas
terraform version
obsutil version
kubectl version --client
\end{code}

\begin{tip}
Utilize \inlinecode{terraform plan} antes de \inlinecode{terraform apply} para
visualizar as alterações de infraestrutura e evitar modificações indesejadas de recursos.
\end{tip}

\subsection{Método de Aceitação}

\begin{table}[H]
  \begin{hutable}{|l|p{0.72\textwidth}|}
    \rowcolor{huaweired} \thd{Resultado} & \thd{Explicação} \\
    \tbody
    Satisfeito & O caso de teste passa completamente — o comportamento observado corresponde ao resultado esperado sem desvios. \\
    Satisfeito com ressalvas & O caso de teste passa, mas desvios menores foram observados que não impactam os objetivos da POC (documentados em Observações). \\
    Não satisfeito & O caso de teste falha — o comportamento observado não corresponde ao resultado esperado. Um relatório de defeito deve ser registrado. \\
    Não testado & O caso de teste não pôde ser executado devido a dependências bloqueantes ou problemas de ambiente. \\
  \end{hutable}
  \caption{Critérios de aceitação.}
\end{table}

\subsection{Resumo dos Casos de Teste}

\begin{testsummary}
  \testsummaryrow{TC-001}{Verificar implantação e saúde do cluster MRS}{\testresultbadge{Pass}}
  \testsummaryrow{TC-002}{Verificar criação e acessibilidade do bucket OBS}{\testresultbadge{Pass}}
  \testsummaryrow{TC-003}{Verificar configuração de VPC e grupo de segurança}{\testresultbadge{Pass}}
  \testsummaryrow{TC-004}{Verificar criação do workspace do DataArts Studio}{\testresultbadge{Pass}}
  \testsummaryrow{TC-005}{Verificar execução do pipeline de ingestão de dados}{\testresultbadge{Pass}}
  \testsummaryrow{TC-006}{Verificar consulta Spark SQL no cluster MRS}{\testresultbadge{Pass}}
  \testsummaryrow{TC-007}{Verificar aplicação de política IAM nos buckets OBS}{\testresultbadge{Pass}}
  \testsummaryrow{TC-008}{Verificar regra de mascaramento de dados do DataArts Studio}{\testresultbadge{Pass}}
  \testsummaryrow{TC-009}{Verificar implantação de infraestrutura via Terraform}{\testresultbadge{Pass}}
  \testsummaryrow{TC-010}{Verificar carregamento de dados de teste no OBS}{\testresultbadge{Pass}}
\end{testsummary}

% =====================================================================
%  Seção 2 — Arquitetura da Plataforma
% =====================================================================
\section{Arquitetura da Plataforma}

\begin{testcase}{TC-001: Verificar implantação e saúde do cluster MRS}
  \testobjective{Confirmar que o cluster MapReduce Service (MRS) está implantado
    com os componentes necessários (HDFS, YARN, Spark, Hive) e que todos os nós
    apresentam status saudável.}
  \testprerequisites{1. Aplicação da infraestrutura Terraform concluída com sucesso. \\ 2. ID do cluster MRS disponível na saída do Terraform. \\ 3. Usuário IAM possui função \textbf{MRS\_Viewer} ou superior.}
  \testprocedure{Ver passos de teste abaixo.}
  \begin{teststeps}
    \teststep{1}{Acessar o Console do Huawei Cloud}{Painel do Console exibido}{\testresultbadge{Pass}}
    \teststep{2}{Navegar em \menu{Console, MapReduce Service, Clusters}}{Página de clusters exibida}{\testresultbadge{Pass}}
    \teststep{3}{Localizar cluster \inlinecode{mrs-poc-cluster}}{Cluster encontrado na lista}{\testresultbadge{Pass}}
    \teststep{4}{Verificar status do cluster}{Status é \textbf{Executando}}{\testresultbadge{Pass}}
    \teststep{5}{Revisar aba Componente}{HDFS, YARN, Spark, Hive todos \textbf{Normal}}{\testresultbadge{Pass}}
    \teststep{6}{Revisar aba Nó}{Todos os nós mostram \textbf{Executando}}{\testresultbadge{Pass}}
  \end{teststeps}

  \image[width=0.8\linewidth]{common-assets/exemplo-menu.png}

  \testexpected{1. Status do cluster é \textbf{Executando}. \\ 2. Todos os quatro componentes (HDFS, YARN, Spark, Hive) estão listados com status \textbf{Normal}. \\ 3. Todos os nós apresentam status \textbf{Executando} sem alarmes.}
  \testremarks{Se algum componente apresentar status \textbf{Anormal}, verificar a página de alarmes do MRS antes de registrar o resultado.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-002: Verificar criação e acessibilidade do bucket OBS}
  \testobjective{Confirmar que o bucket do Object Storage Service (OBS) utilizado
    para o data lake da POC está criado, possui a classe de armazenamento correta
    e é acessível com a política IAM designada.}
  \testprerequisites{1. Nome do bucket OBS definido na configuração Terraform. \\ 2. Política IAM concedendo acesso de leitura/escrita ao bucket está anexada ao usuário de teste.}
  \testprocedure{1. Acessar o Console. \\ 2. Navegar em \menu{Console, Object Storage Service}. \\ 3. Localizar o bucket (ex.: \inlinecode{poc-datalake-raw}). \\ 4. Verificar se a classe de armazenamento é \textbf{Standard}. \\ 5. Carregar um arquivo de teste (\inlinecode{test-upload.txt}) no bucket. \\ 6. Baixar o arquivo e comparar seu conteúdo com o original. \\ 7. Excluir o arquivo de teste.}
  \testexpected{1. O bucket existe com classe de armazenamento \textbf{Standard}. \\ 2. O carregamento do arquivo conclui sem erro. \\ 3. O conteúdo do arquivo baixado corresponde ao original. \\ 4. A exclusão do arquivo é realizada com sucesso.}
  \testremarks{A consistência eventual do OBS pode causar um breve atraso antes que um objeto recém-carregado apareça nas listagens.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-003: Verificar configuração de VPC e grupo de segurança}
  \testobjective{Confirmar que a VPC, a sub-rede e os grupos de segurança estão
    configurados conforme o diagrama de arquitetura de rede, com os blocos CIDR
    e regras de entrada/saída corretos.}
  \testprerequisites{1. IDs da VPC e da sub-rede disponíveis na saída do Terraform. \\ 2. Nomes dos grupos de segurança documentados na arquitetura da solução.}
  \testprocedure{1. Acessar o Console. \\ 2. Navegar em \menu{Console, Virtual Private Cloud, VPCs}. \\ 3. Localizar a VPC (ex.: \inlinecode{vpc-poc}) e verificar se o bloco CIDR corresponde à arquitetura (ex.: \inlinecode{10.0.0.0/16}). \\ 4. Clicar na sub-rede e verificar seu CIDR (ex.: \inlinecode{10.0.1.0/24}). \\ 5. Navegar até \textbf{Security Groups} e verificar regras de entrada: TCP 22 da sub-rede bastion, TCP 443 do proxy corporativo. \\ 6. Verificar se as regras de saída permitem todo o tráfego (padrão).}
  \testexpected{1. CIDR da VPC é \inlinecode{10.0.0.0/16}. \\ 2. CIDR da sub-rede é \inlinecode{10.0.1.0/24}. \\ 3. Regras de entrada correspondem à arquitetura: TCP 22 e TCP 443 das origens especificadas. \\ 4. Saída permite todo o tráfego.}
  \testremarks{Se o grupo de segurança possuir regras adicionais além da especificação da arquitetura, documentá-las no campo de Observações.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

% =====================================================================
%  Seção 3 — Engenharia de Dados
% =====================================================================
\section{Engenharia de Dados}

\begin{testcase}{TC-004: Verificar criação do workspace do DataArts Studio} \badge{Obrigatório}
  \testobjective{Confirmar que a instância do DataArts Studio (Dayu) está
    provisionada, o workspace está acessível e os usuários IAM designados
    conseguem acessar com a função \textbf{Dayu\_User}.}
  \testprerequisites{1. Instância do DataArts Studio provisionada e com status \textbf{Executando}. \\ 2. Usuário IAM de teste possui a função \textbf{Dayu\_User} atribuída.}
  \testprocedure{1. Acessar o Console com o usuário IAM de teste. \\ 2. Navegar em \menu{Console, DataArts Studio, Workspaces}. \\ 3. Localizar o workspace (ex.: \inlinecode{poc-workspace}). \\ 4. Clicar em \textbf{Acessar Workspace} para abrir o console do DataArts Studio. \\ 5. Verificar se o painel de navegação à esquerda carrega com os módulos esperados: \textbf{Integração de Dados}, \textbf{Desenvolvimento de Dados}, \textbf{Arquitetura de Dados}.}
  \testexpected{1. O workspace está listado e acessível. \\ 2. O console do DataArts Studio abre sem erros. \\ 3. Todos os três módulos (Integração de Dados, Desenvolvimento de Dados, Arquitetura de Dados) estão visíveis na navegação.}
  \testremarks{Se o workspace falhar ao carregar, verificar o status da instância Dayu e a atribuição da função IAM.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-005: Verificar execução do pipeline de ingestão de dados}
  \testobjective{Confirmar que um pipeline em lote do DataArts Studio consegue
    ingerir dados CSV do OBS, aplicar uma transformação básica
    e gravar o resultado no caminho OBS de destino.}
  \testprerequisites{1. TC-004 concluído (workspace do DataArts Studio acessível). \\ 2. Arquivo CSV de origem (\inlinecode{customers.csv}) existe no bucket OBS raw. \\ 3. Caminho OBS de destino (\inlinecode{poc-datalake-curated/customers/}) está configurado. \\ 4. Pipeline \inlinecode{pl-ingest-customers} está publicado no DataArts Studio.}
  \testprocedure{Ver passos de teste abaixo.}
  \begin{teststeps}
    \teststep{1}{Navegar até Desenvolvimento de Dados no DataArts Studio}{Página de Desenvolvimento de Dados carrega}{\testresultbadge{Pass}}
    \teststep{2}{Localizar pipeline \inlinecode{pl-ingest-customers}}{Pipeline encontrado na lista de jobs}{\testresultbadge{Pass}}
    \teststep{3}{Clicar em Executar para iniciar o pipeline}{Pipeline inicia execução}{\testresultbadge{Pass}}
    \teststep{4}{Aguardar status do pipeline (timeout: 10 min)}{Status muda para \textbf{Sucesso}}{\testresultbadge{Pass}}
    \teststep{5}{Verificar arquivo de saída no OBS}{Arquivo existe no caminho de destino}{\testresultbadge{Pass}}
    \teststep{6}{Baixar e verificar contagem de linhas}{Contagem de linhas corresponde à origem}{\testresultbadge{Pass}}
  \end{teststeps}

  \begin{code}[bash]
# Verificar o arquivo de saída no OBS via CLI
obsutil ls obs://poc-datalake-curated/customers/ -limit=10
obsutil stat obs://poc-datalake-curated/customers/output.csv
  \end{code}

  \testexpected{1. A execução do pipeline conclui com status \textbf{Sucesso}. \\ 2. O arquivo de saída é criado no caminho OBS de destino. \\ 3. A contagem de linhas do arquivo de saída corresponde ao arquivo de origem.}
  \testremarks{O tempo de execução do pipeline varia conforme o volume de dados. Para a POC, o arquivo de origem contém aproximadamente 10.000 linhas.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-006: Verificar consulta Spark SQL no cluster MRS}
  \testobjective{Confirmar que uma consulta Spark SQL pode ser executada no
    cluster MRS para ler dados de uma tabela Hive e retornar resultados
    corretos, validando a integração entre o MRS e o data lake.}
  \testprerequisites{1. TC-001 concluído (cluster MRS saudável). \\ 2. Tabela Hive \inlinecode{poc\_db.customers} existe e contém dados carregados pelo pipeline de ingestão. \\ 3. Acesso SSH ao nó master do MRS configurado.}
  \testprocedure{1. Acessar o nó master do MRS via SSH. \\ 2. Executar a CLI do Spark SQL: \inlinecode{spark-sql --master yarn -{}-conf spark.sql.hive.convertMetastoreOrc=true -e "SELECT COUNT(*) FROM poc\_db.customers;"} \\ 3. Verificar se a contagem retornada corresponde ao esperado (10.000). \\ 4. Executar uma consulta de amostra para verificar a integridade dos dados: \inlinecode{spark-sql --master yarn -e "SELECT customer\_id, name FROM poc\_db.customers LIMIT 5;"} \\ 5. Confirmar que a consulta retorna 5 linhas com valores não nulos.}

  \begin{code}[bash]
# Acessar o nó master do MRS via SSH
ssh root@mrs-master-poc

# Executar consulta de contagem Spark SQL
spark-sql --master yarn \
  --conf spark.sql.hive.convertMetastoreOrc=true \
  -e "SELECT COUNT(*) FROM poc_db.customers;"
  \end{code}

  \testexpected{1. \inlinecode{COUNT(*)} retorna 10.000. \\ 2. A consulta de amostra retorna 5 linhas com valores válidos de \inlinecode{customer\_id} e \inlinecode{name}.}
  \testremarks{Se a CLI do Spark SQL não estiver disponível, utilizar a interface web do componente Spark2x do MRS para submeter a consulta.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

% =====================================================================
%  Seção 4 — Segurança e Controle de Acesso
% =====================================================================
\section{Segurança e Controle de Acesso}

\begin{testcase}{TC-007: Verificar aplicação de política IAM nos buckets OBS} \badge{Novo}
  \testobjective{Confirmar que as políticas IAM restringem corretamente o acesso
    ao bucket OBS — usuários autorizados podem ler/escrever enquanto usuários
    não autorizados são negados, e a política do bucket segue o princípio
    do menor privilégio.}
  \testprerequisites{1. Bucket OBS \inlinecode{poc-datalake-raw} existe com a política IAM designada. \\ 2. Dois usuários IAM de teste: um com função \textbf{OBS\_ReadWrite}, outro sem permissões OBS. \\ 3. CLI \inlinecode{obsutil} configurada para ambos os usuários.}
  \testprocedure{1. Usando o usuário autorizado, carregar um arquivo de teste em \inlinecode{poc-datalake-raw}. \\ 2. Usando o usuário autorizado, baixar e verificar o conteúdo do arquivo. \\ 3. Usando o usuário não autorizado, tentar carregar um arquivo — verificar se a requisição é negada (HTTP 403). \\ 4. Usando o usuário não autorizado, tentar listar objetos no bucket — verificar se a requisição é negada. \\ 5. Revisar a política do bucket no Console em \menu{Console, Object Storage Service, Bucket Policies} e confirmar que concede apenas as permissões pretendidas.}

  \begin{code}[bash]
# Usuário autorizado — carregar e verificar
obsutil cp test-file.txt obs://poc-datalake-raw/test-file.txt
obsutil ls obs://poc-datalake-raw/ -limit=5

# Usuário não autorizado — esperar erros 403
obsutil cp test-file.txt obs://poc-datalake-raw/unauthorized.txt
obsutil ls obs://poc-datalake-raw/
  \end{code}

  \testexpected{1. Usuário autorizado consegue carregar e baixar com sucesso. \\ 2. Usuário não autorizado recebe HTTP 403 nas operações de carregamento e listagem. \\ 3. Política do bucket concede apenas as permissões de leitura/escrita pretendidas às funções designadas.}
  \testremarks{Verificar os logs do Cloud Trail se a negação de acesso não for retornada conforme esperado — pode haver herança de política do projeto ou domínio.}

  \note{Alterações em políticas IAM podem levar até 5 minutos para propagar. Aguardar antes de retestar após modificar uma política.}

  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-008: Verificar regra de mascaramento de dados do DataArts Studio}
  \testobjective{Confirmar que colunas de dados sensíveis (ex.: campos PII) são
    mascaradas conforme a regra de mascaramento do DataArts Studio quando
    consultadas por usuários não administradores.}
  \testprerequisites{1. TC-004 concluído (workspace do DataArts Studio acessível). \\ 2. Regra de mascaramento configurada para a coluna \inlinecode{customers.ssn} (Número de Seguro Social). \\ 3. Usuário de teste possui função \textbf{Dayu\_User} (não administrador).}
  \testprocedure{1. No console do DataArts Studio, navegar até \textbf{Arquitetura de Dados} $\rightarrow$ \textbf{Mascaramento de Dados}. \\ 2. Verificar se a regra de mascaramento para \inlinecode{customers.ssn} está ativa e utiliza a estratégia \textbf{Mascarar Tudo}. \\ 3. Consultar a tabela \inlinecode{customers} como usuário de teste via console SQL do DataArts Studio. \\ 4. Verificar se a coluna \inlinecode{ssn} retorna valores mascarados (ex.: \inlinecode{*********}) em vez dos dados reais. \\ 5. Consultar a mesma tabela como usuário administrador e verificar se os valores reais são retornados.}
  \testexpected{1. Regra de mascaramento ativa para \inlinecode{customers.ssn}. \\ 2. Usuário não administrador visualiza valores mascarados na coluna \inlinecode{ssn}. \\ 3. Usuário administrador visualiza valores reais na coluna \inlinecode{ssn}.}
  \testremarks{Regras de mascaramento de dados são aplicadas no momento da consulta — os dados subjacentes no OBS/HDFS não são modificados.}
  \testresult{\testresultbadge{Pass}}
\end{testcase}

% =====================================================================
%  Seção 5 — Configuração do Ambiente e Ferramentas
% =====================================================================
\section{Configuração do Ambiente e Ferramentas}

\begin{testcase}{TC-009: Verificar implantação de infraestrutura via Terraform}
  \testobjective{Confirmar que a configuração Terraform implanta todos os
    recursos de infraestrutura necessários (VPC, MRS, OBS, DataArts Studio)
    sem erros e que o arquivo de estado reflete os recursos esperados.}
  \testprerequisites{1. CLI \inlinecode{terraform} instalada (versão $\geq$ 1.0). \\ 2. Credenciais AK/SK configuradas via variáveis de ambiente ou \param{provider.tf}. \\ 3. Arquivos de configuração Terraform no diretório do projeto.}
  \testprocedure{1. Navegar até o diretório do projeto Terraform. \\ 2. Executar \inlinecode{terraform init} para inicializar o diretório de trabalho. \\ 3. Executar \inlinecode{terraform plan} e revisar as alterações planejadas — confirmar que todos os recursos esperados estão listados. \\ 4. Executar \inlinecode{terraform apply -auto-approve} e aguardar a conclusão. \\ 5. Executar \inlinecode{terraform output} e verificar se todos os valores de saída estão preenchidos. \\ 6. Executar \inlinecode{terraform state list} e confirmar que a contagem de recursos corresponde à arquitetura.}

  \begin{code}[bash]
# Inicializar e implantar infraestrutura
cd /opt/poc-infra/terraform
terraform init
terraform plan -out=tfplan
terraform apply tfplan

# Verificar implantação
terraform output
terraform state list | wc -l
  \end{code}

  \testexpected{1. \inlinecode{terraform apply} conclui sem erros. \\ 2. Todos os valores de saída estão preenchidos (ID do cluster, nome do bucket, ID da VPC). \\ 3. Contagem de recursos no estado corresponde à especificação da arquitetura.}
  \testremarks{Se \inlinecode{terraform apply} falhar, verificar as credenciais AK/SK e os limites de cota da região \inlinecode{sa-brazil-1}.}

  \begin{warning}
  Não pule a etapa de revisão do \inlinecode{terraform plan}. Aplicar alterações
  não revisadas em ambientes adjacentes à produção pode causar perda de dados
  ou interrupção de serviços.
  \end{warning}

  \testresult{\testresultbadge{Pass}}
\end{testcase}

\begin{testcase}{TC-010: Verificar carregamento de dados de teste no OBS}
  \testobjective{Confirmar que todos os arquivos de dados de teste necessários
    (CSV, JSON) podem ser carregados no bucket OBS designado e são acessíveis
    pelo pipeline do DataArts Studio.}
  \testprerequisites{1. TC-002 concluído (bucket OBS acessível). \\ 2. Arquivos de dados de teste preparados no diretório local \param{./test-data/}. \\ 3. CLI \inlinecode{obsutil} configurada com as credenciais do usuário de teste.}
  \testprocedure{1. Listar os arquivos de dados de teste locais e verificar sua integridade (contagem de linhas, esquema). \\ 2. Carregar todos os arquivos no bucket OBS raw usando \inlinecode{obsutil}. \\ 3. Verificar se cada arquivo existe no OBS listando o conteúdo do bucket. \\ 4. Baixar um arquivo de amostra do OBS e compará-lo com o original local (checksum). \\ 5. No DataArts Studio, verificar se a conexão de fonte de dados consegue ler os arquivos carregados.}

  \begin{code}[bash]
# Carregar arquivos de dados de teste no OBS
obsutil cp ./test-data/customers.csv obs://poc-datalake-raw/customers.csv
obsutil cp ./test-data/transactions.json obs://poc-datalake-raw/transactions.json

# Verificar carregamentos
obsutil ls obs://poc-datalake-raw/ -limit=20
  \end{code}

  \testexpected{1. Todos os arquivos de dados de teste são carregados sem erros. \\ 2. A listagem do bucket mostra todos os arquivos carregados com tamanhos corretos. \\ 3. O checksum do arquivo baixado corresponde ao original local. \\ 4. A conexão de fonte de dados do DataArts Studio consegue ler os arquivos.}
  \testremarks{Arquivos grandes ($>$1 GB) devem ser carregados usando o recurso de upload multipart do OBS para maior confiabilidade.}

  \note{Consulte a \weblink{https://support.huaweicloud.com/obs/index.html}{Documentação do Huawei Cloud OBS} para opções avançadas de upload e solução de problemas.}

  \testresult{\testresultbadge{Pass}}
\end{testcase}

\imageplaceholder{assets/test-execution-dashboard.png}{Captura de tela do painel de execução de testes mostrando os resultados de todos os casos de teste}

% =====================================================================
%  Histórico de versões
% =====================================================================
\begin{changelog}
  \changelogentry{1.1.0}{\today}{
    \item Adicionadas demonstrações abrangentes de recursos: caixas de destaque \inlinecode{warning}, \inlinecode{tip}, \inlinecode{infobox}; blocos \inlinecode{code}; \inlinecode{badge}; \inlinecode{weblink}; \inlinecode{note}; \inlinecode{param}; \inlinecode{image}, \inlinecode{imagecap} e \inlinecode{imageplaceholder}; caminhos \inlinecode{menu}; e referências \inlinecode{inlinecode}.
    \item Adicionados casos de teste de Segurança e Controle de Acesso (TC-007, TC-008).
    \item Adicionados casos de teste de Configuração do Ambiente e Ferramentas (TC-009, TC-010).
  }
  \changelogentry{1.0.0}{\today}{
    \item Versão inicial.
    \item Adicionados casos de teste de Arquitetura da Plataforma (TC-001 a TC-003).
    \item Adicionados casos de teste de Engenharia de Dados (TC-004 a TC-006).
  }
\end{changelog}

\end{document}
